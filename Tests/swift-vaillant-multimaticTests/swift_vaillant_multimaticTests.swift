import XCTest
@testable import swift_vaillant_multimatic

final class swift_vaillant_multimaticTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_vaillant_multimatic().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
